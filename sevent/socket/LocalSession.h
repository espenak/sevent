#pragma once

namespace sevent
{
    namespace socket
    {

        class LocalSession
        {
            public:
                LocalSession();
                virtual ~LocalSession();
        };

    } // namespace socket
} // namespace sevent
